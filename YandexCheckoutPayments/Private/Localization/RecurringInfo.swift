enum RecurringInfoLocalization {
    enum Wallet: String {
        case header = "RecurringInfo.Wallet.Header"
        case body = "RecurringInfo.Wallet.Body"
    }
    enum BankCard: String {
        case header = "RecurringInfo.BankCard.Header"
        case body = "RecurringInfo.BankCard.Body"
    }
}
